import 'package:flutter/material.dart';

class FullArticleView extends StatelessWidget {
  const FullArticleView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
